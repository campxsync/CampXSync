<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.campxsync</groupId>
        <artifactId>campxsync-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>logger</artifactId>
    <!-- Version inherited from campxsync-parent (1.0.0-SNAPSHOT) -->
    <packaging>jar</packaging>

    <name>CampXSync Shared Logging Library</name>
    <description>Shared library for structured logging, audit trails, encryption, caching, validation, and JWT authentication context handling.</description>

    <properties>
        <!-- maven.compiler.source/target and encoding are inherited from campxsync-parent via spring-boot-starter-parent -->

        <!-- Logger-specific dependency versions not managed by parent BOM -->
        <slf4j.version>1.7.36</slf4j.version>
        <logback.version>1.2.13</logback.version>
        <jackson.version>2.13.5</jackson.version>
        <java-jwt.version>4.4.0</java-jwt.version>
        <junit.version>5.8.2</junit.version>
    </properties>

    <dependencies>
        <!-- Logging API and Backend -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
        </dependency>

        <!-- JWT handling -->
        <dependency>
            <groupId>com.auth0</groupId>
            <artifactId>java-jwt</artifactId>
            <version>${java-jwt.version}</version>
        </dependency>

        <!-- JSON Serialization -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
            <version>${jackson.version}</version>
        </dependency>

        <!-- AspectJ AOP for Method Entry/Exit Logging -->
        <dependency>
            <groupId>org.aspectj</groupId>
            <artifactId>aspectjrt</artifactId>
            <version>1.9.7</version>
        </dependency>
        <dependency>
            <groupId>org.aspectj</groupId>
            <artifactId>aspectjweaver</artifactId>
            <version>1.9.7</version>
        </dependency>

        <!-- Unit Testing -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <!--
        No <build><plugins> block needed here. The compiler (maven-compiler-plugin) and
        surefire (maven-surefire-plugin) are fully managed by campxsync-parent via
        pluginManagement and spring-boot-starter-parent.
        NOTE: spring-boot-maven-plugin is intentionally NOT declared here because
        'logger' is a shared library (plain JAR), not a Spring Boot application.
    -->
</project>
